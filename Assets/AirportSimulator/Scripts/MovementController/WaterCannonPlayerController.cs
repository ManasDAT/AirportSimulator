using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaterCannonPlayerController : MonoBehaviour {

	public float speed;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update ()
	{
		////float horizontal = Input.GetAxis ("Left_Joystick_Horizontal");
		//float vertical = Input.GetAxis ("Left_Joystick_Vertical");
		//Vector3 direction = new Vector3 (horizontal, 0, vertical);
		//transform.Translate (direction.normalized * Time.deltaTime * speed);

		//Debug.Log (Input.GetAxis ("Left_Joystick_Horizontal"));
	}
}
